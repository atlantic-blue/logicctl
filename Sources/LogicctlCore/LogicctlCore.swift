/// The part of logicctl that every other part is built on.
///
/// This module holds the envelope, the error codes, the state and the rules a command goes
/// through. It carries a placeholder until the step that adds the envelope.
public enum LogicctlCore {
  /// The name of this module.
  public static let moduleName = "LogicctlCore"
}
